/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { WatchService } from "../watch.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { WatchCreateInput } from "./WatchCreateInput";
import { WatchWhereInput } from "./WatchWhereInput";
import { WatchWhereUniqueInput } from "./WatchWhereUniqueInput";
import { WatchFindManyArgs } from "./WatchFindManyArgs";
import { WatchUpdateInput } from "./WatchUpdateInput";
import { Watch } from "./Watch";
import { Post } from "../../post/base/Post";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class WatchControllerBase {
  constructor(
    protected readonly service: WatchService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Watch })
  @nestAccessControl.UseRoles({
    resource: "Watch",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async create(@common.Body() data: WatchCreateInput): Promise<Watch> {
    return await this.service.create({
      data: data,
      select: {
        change: true,
        contentType: true,
        cPrice: true,
        createdAt: true,
        id: true,
        scrip: true,
        soldAt: true,
        sPrice: true,
        updatedAt: true,
        userId: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [Watch] })
  @ApiNestedQuery(WatchFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "Watch",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findMany(@common.Req() request: Request): Promise<Watch[]> {
    const args = plainToClass(WatchFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        change: true,
        contentType: true,
        cPrice: true,
        createdAt: true,
        id: true,
        scrip: true,
        soldAt: true,
        sPrice: true,
        updatedAt: true,
        userId: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Watch })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Watch",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findOne(
    @common.Param() params: WatchWhereUniqueInput
  ): Promise<Watch | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        change: true,
        contentType: true,
        cPrice: true,
        createdAt: true,
        id: true,
        scrip: true,
        soldAt: true,
        sPrice: true,
        updatedAt: true,
        userId: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Watch })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Watch",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async update(
    @common.Param() params: WatchWhereUniqueInput,
    @common.Body() data: WatchUpdateInput
  ): Promise<Watch | null> {
    try {
      return await this.service.update({
        where: params,
        data: data,
        select: {
          change: true,
          contentType: true,
          cPrice: true,
          createdAt: true,
          id: true,
          scrip: true,
          soldAt: true,
          sPrice: true,
          updatedAt: true,
          userId: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Watch })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Watch",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async delete(
    @common.Param() params: WatchWhereUniqueInput
  ): Promise<Watch | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          change: true,
          contentType: true,
          cPrice: true,
          createdAt: true,
          id: true,
          scrip: true,
          soldAt: true,
          sPrice: true,
          updatedAt: true,
          userId: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
